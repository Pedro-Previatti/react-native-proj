import { StyleSheet } from "react-native";

export default StyleSheet.create({
  text: {
    fontSize: 20,
    fontWeight: "bold",
    borderColor: "blue",
    borderWidth: 2,
    padding: 15,
  },
});
